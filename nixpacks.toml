[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['yarn install --frozen-lockfile']

[phases.build]
cmds = ['yarn build']

[start]
cmd = 'yarn start'

[variables]
NODE_ENV = 'production'
NEXT_TELEMETRY_DISABLED = '1'
NODE_OPTIONS = '--max-old-space-size=4096'
